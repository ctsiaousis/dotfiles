#!/bin/bash

[[ $1 =~ ^-[0-9]+$ ]] && $(sudo light -U ${1//-}) || $(sudo light -A ${1//+})

title=$(echo "Brightness Updated")

notify-send -i $HOME/.face "$title" "with value: <span color='#57dafd' font='26px'><i><b>$(printf '%.f\n' $(light -G))</b></i></span>" -u low -t 2000

